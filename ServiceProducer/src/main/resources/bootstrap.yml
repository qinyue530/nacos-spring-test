server:
  port: 8086
  tomcat:
    max-threads: 100
spring:
  application:
    name: ProduceService
  cloud:
    nacos:
      discovery:
        server-addr: idea.nginx.nacos:81
        namespace:
      #        ip: 127.0.0.1
      config:
        server-addr: idea.nginx.nacos:81
      #  sentinel:
      #transport:
      #dashboard: 127.0.0.1:8719
    #eager: true
management:
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    export:
      prometheus:
        enabled: true