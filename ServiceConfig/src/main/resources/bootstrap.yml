server:
  port: 8082
spring:
  application:
    name: test  #Date Id
  cloud:
    nacos:
      discovery:
        server-addr: 129.204.22.71:8848
      config:
        server-addr: 129.204.22.71:8848
        group: DEFAULT_GROUP
        file-extension: yaml #nacos的配置列表 dataId 必须有后缀 且与配置格式相同
  datasource:
    url: jdbc:mysql://129.204.22.71:3306/swtest
    username: root
    password: 65iDhDFx6cKpPzfp
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  type-aliases-package: com.nacos.dao
